# Global ignore file - overrides .gitignore patterns
# Used by ripgrep, fd, and other tools that respect .ignore
# Whitelist patterns to INCLUDE files that are typically gitignored

# Environment files (usually gitignored but needed in searches)
!.env
!.env.*
!.env.local
!.env.example
!.env.development
!.env.production
