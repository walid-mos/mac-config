#compdef gf

_gf() {
    local ret=1
    _arguments \
        '-i[Interactive mode - confirm each branch]' \
        '--interactive[Interactive mode - confirm each branch]' \
        '-h[Show help]' \
        '--help[Show help]' && ret=0
    return ret
}

_gf "$@"
