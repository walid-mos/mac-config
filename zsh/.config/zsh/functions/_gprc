#compdef gprc

_gprc() {
    local ret=1
    _arguments \
        '-u[Update description of existing PR]' \
        '--update-description[Update description of existing PR]' \
        '-h[Show help]' \
        '--help[Show help]' && ret=0
    return ret
}

_gprc "$@"
